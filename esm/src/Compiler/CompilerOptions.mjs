import Logger from"../Logger/Logger.mjs";export default class CompilerOptions{constructor(e={}){if(null===e||"object"!=typeof e)throw new TypeError("Expected options to be an object.");if(void 0===e.logger)this.logger=new Logger({namespace:"TRANSAX:COMPILER"});else if(e.logger instanceof Logger)this.logger=e.logger;else{if(null===e.logger||"object"!=typeof e.logger)throw new TypeError("Expected options.logger to be an instance of LoggerInterface or LoggerOptionsType.");this.logger=new Logger(e.logger)}}}