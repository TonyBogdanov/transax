import v_switch from './switch.js';
import v_safe_expression from './safe_expression.js';

export default {

    name: 'expression',
    tests: [
        v_switch,
        v_safe_expression,
    ],

};
