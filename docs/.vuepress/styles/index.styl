.hero {
  margin: 5rem 0 7rem;
}

#main-title {
  display: inline-block;
  font-size: 4rem;
  font-weight: 700;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  background-image: linear-gradient( 90deg, #3eaf7c, #00cae9 );
  background-clip: text;
  -webkit-text-fill-color: transparent;
}
